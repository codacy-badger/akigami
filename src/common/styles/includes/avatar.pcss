.avatar {
    display: block;
    border-radius: 50%;
    background-color: $color-primary-dark;
    background-size: cover;

    &.online {
        box-shadow: 0 0 0 3px $color-online;
    }

    &.offline {
        box-shadow: 0 0 0 3px $color-offline;
    }

}