import light from './light';
import mixins from './mixins';

export default {
  fontFamily: 'Google Sans, sans-serif',
  altFontFamily: 'Futura PT, sans-serif',
  fontSize: 16,
  colors: light,
  mixins,
};
