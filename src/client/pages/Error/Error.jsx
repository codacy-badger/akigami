import React, { PureComponent } from 'react';

class Error extends PureComponent {
    render() {
        return (
            <div>
                error
            </div>
        );
    }
}

export default Error;
