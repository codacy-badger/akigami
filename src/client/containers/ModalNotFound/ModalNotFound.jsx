import React, { PureComponent } from 'react';

export default class ModalNotFound extends PureComponent {
    render() {
        return (
            <div>
                Modal not found
            </div>
        )
    }
}